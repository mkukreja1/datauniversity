<coordinator-app name="Student_Data_Pipeline_Coord" frequency="${frequency}" start="${startTime}" end="${endTime}" timezone="${timeZone}" xmlns="uri:oozie:coordinator:0.1">
   <action>
         <workflow>
                  <app-path>${workflowRoot}/workflow.xml</app-path>
         </workflow>
  </action>
</coordinator-app>

